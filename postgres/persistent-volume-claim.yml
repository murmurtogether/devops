kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: postgres-pv-claim
  labels:
    app: postgres
spec:
  storageClassName: cloudru-nvme
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 3Gi
